import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  emptyContainer: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
    marginTop: 20,
  },
  emptyText: {
    fontSize: 18,
    color: "#666666",
    fontWeight: "500",
  },
  hintText: {
    fontSize: 14,
    color: "#999999",
    marginTop: 8,
  },
})
