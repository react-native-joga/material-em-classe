import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  input: {
    height: 40,
    borderColor: "#CCCCCC",
    borderWidth: 1,
    borderRadius: 4,
    paddingHorizontal: 8,
    marginBottom: 12,
    fontSize: 16,
  },
})
